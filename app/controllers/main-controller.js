(function( define ) {
  'use strict';

  define( [], function ()
  {
    var MainController = function( $rootScope, $scope ){


    };

    return [ '$rootScope', '$scope', MainController ];

  });

}( define ));
